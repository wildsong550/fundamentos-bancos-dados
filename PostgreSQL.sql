create table usuario(
  id_usuario INTEGER primary key not NULL,
  nome VARCHAR(100)
  idade INTEGER
  );
  
create table aluno(
  id_aluno INTEGER PRIMARY KEY NOT NULL,
  nome VARCHAR(250)
  idade INTEGER,
  ano  INTEGER,
  periodo INTEGER,
  )